## Create ab initial project

1. install npm command 
- Node package manger on appropriate OS environement. In this case, use MacOS.

# The easiest way to install Node.js and npm is via Homebrew : 
    - brew install node 
        - /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)" 
    - node -v
    - npm -v
    - npm install : this creates a file called package-lock.json
    - docker build . 